<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:id="@+id/base_items">
        <item
            android:id="@+id/nav_item_home"
            android:icon="@android:drawable/ic_menu_agenda"
            android:title="@string/menu_home" />

        <item
            android:id="@+id/nav_item_your_music"
            android:icon="@android:drawable/ic_menu_agenda"
            android:title="@string/menu_your_music" />

        <item
            android:id="@+id/nav_playlist"
            android:icon="@android:drawable/ic_menu_agenda"
            android:title="@string/menu_playlist" />
    </group>

    <group android:id="@+id/other_items">
        <item
            android:id="@+id/nav_item_about"
            android:icon="@android:drawable/ic_menu_agenda"
            android:title="@string/menu_about" />

        <item
            android:id="@+id/nav_item_logout"
            android:icon="@android:drawable/ic_menu_agenda"
            android:title="@string/menu_item_logout" />
    </group>

</menu>